import React from "react";

const Answer = ({name, place}) => {
    return <div>Hello I'm {name} and From {place}</div>
}
// }
// const Answer = (props) => {
//     return <div>Hello I'm {props.name} and I'm from {props.from}</div>
// };
export default Answer;
